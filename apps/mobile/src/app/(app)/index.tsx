import { TrackerScreen } from '../../features/tracker/tracker-screen';

/** The Tracker is the primary screen and the app's landing route (PRD §19). */
export default function TrackerRoute() {
  return <TrackerScreen />;
}
